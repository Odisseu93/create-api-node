# NestJS API

Generated by [CNA - Create Node API](https://github.com/your-org/create-node-api). Built with [NestJS](https://docs.nestjs.com/).

## Setup

```bash
npm install
```

## Scripts

- `npm run start:dev` — Start in watch mode (recommended for development)
- `npm run build` — Build for production
- `npm run start` — Run production build
- `npm run lint` / `npm run lint:fix` — ESLint
- `npm run format` — Prettier
- `npm test` / `npm run test:watch` — Jest

## Endpoints

- `GET /` — Hello World
- `GET /health` — Health check `{ "status": "ok" }`

## Docs

- [NestJS Documentation](https://docs.nestjs.com/)
